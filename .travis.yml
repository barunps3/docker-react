language: generic
sudo: required
services:
  - docker 

before_install:
  - docker build -t barunps3/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true barunps3/docker-react npm run test 